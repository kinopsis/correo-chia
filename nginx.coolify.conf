# ============================================================
# Nginx Config — Coolify (Frontend + Proxy MinIO)
# Sistema de Atencion Ciudadana — Alcaldia de Chia
# ============================================================
# Sirve el frontend estatico (SPA) y proxyfica MinIO.
# Supabase se consume directamente desde el navegador
# via batppagyceeazffjduxk.supabase.co (Cloud).

server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    location /health {
        return 200 '{"status":"ok","service":"chia-frontend"}';
        add_header Content-Type application/json;
    }

    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss;
    gzip_min_length 1000;
    gzip_vary on;

    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    location /assets/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        access_log off;
    }

    location = /manifest.json {
        expires 7d;
        add_header Cache-Control "public";
    }

    location = /sw.js {
        expires 0;
        add_header Cache-Control "no-cache";
        add_header Service-Worker-Allowed "/";
    }

    location / {
        try_files $uri $uri/ /index.html;
        expires 0;
        add_header Cache-Control "no-cache, must-revalidate";
    }

    location ~ /\. {
        deny all;
        access_log off;
        log_not_found off;
    }
}
